---
import Layout from '../../layouts/Layout.astro';
import ProjectDetailPage from '../../components/react/ProjectDetailPage.tsx';
import { getProjectById, projects } from '../../data/projects';

export async function getStaticPaths() {
  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project },
  }));
}

const { project } = Astro.props;

if (!project) {
  return Astro.redirect('/du-an');
}

const title = `${project.title} - Dự án solar24h.com`;
const description = project.description;
const keywords = [
  project.title,
  project.client,
  project.category,
  'dự án hệ thống điện mặt trời',
  'lắp đặt điện mặt trời',
  'lắp đặt hệ thống solar',
  'SOLAR24H',
  ...project.tags
];
---

<Layout 
  title={title}
  description={description}
  keywords={keywords}
  type="article"
  canonical={`https://solar24h.com/du-an/${project.id}`}
  image={project.images.thumbnail}
  imageAlt={project.title}
>
  <ProjectDetailPage project={project} client:load />
</Layout>