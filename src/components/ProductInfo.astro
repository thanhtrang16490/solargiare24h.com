---

import ProductBuyNowIsland from './react/ProductBuyNowIsland';


interface Props {
  product: {
    name?: string;
    brand?: string | { title?: string };
    price?: number;
    original_price?: number;
    discount_percentage?: number;
    description?: string;
    image_url?: string;
    stock_status?: string;
    status?: string;
    inventory_quantity?: number;
    stock_quantity?: number;
  };
  availability?: string;
}

const { product, availability = 'in_stock' } = Astro.props;

// Xử lý tên thương hiệu
const brandName = typeof product.brand === 'string' 
  ? product.brand 
  : product.brand?.title || '';

// Format giá tiền
const formatCurrency = (price: number) => {
  return new Intl.NumberFormat('vi-VN', {
    style: 'currency',
    currency: 'VND',
    minimumFractionDigits: 0,
  }).format(price);
};

// Get availability display info
const getAvailabilityInfo = (availability: string) => {
  switch (availability) {
    case 'in_stock':
      return {
        text: 'Còn hàng',
        color: 'text-green-600',
        bgColor: 'bg-green-100',
        icon: '✓'
      };
    case 'limited_availability':
      return {
        text: 'Sắp hết hàng',
        color: 'text-orange-600',
        bgColor: 'bg-orange-100',
        icon: '⚠'
      };
    case 'out_of_stock':
      return {
        text: 'Hết hàng',
        color: 'text-red-600',
        bgColor: 'bg-red-100',
        icon: '✗'
      };
    default:
      return {
        text: 'Còn hàng',
        color: 'text-green-600',
        bgColor: 'bg-green-100',
        icon: '✓'
      };
  }
};

const availabilityInfo = getAvailabilityInfo(availability);

---

<div class="space-y-6 sticky top-20 self-start">
  <!-- Tên sản phẩm và thương hiệu -->
  <div>
    <h1 class="text-3xl font-bold text-gray-900">{product.name}</h1>
    {brandName && (
      <div class="text-sm text-gray-600 mt-1">{brandName}</div>
    )}
  </div>

  <!-- Giá sản phẩm -->
  <div class="flex items-baseline gap-4">
    <span class="text-3xl font-bold text-gray-900">
      {product.price ? formatCurrency(product.price) : 'Liên hệ'}
    </span>
    {product.original_price && product.original_price > (product.price || 0) && (
      <span class="text-lg text-gray-500 line-through">
        {formatCurrency(product.original_price)}
      </span>
    )}
    {product.discount_percentage && (
      <span class="text-sm font-medium text-red-600">
        -{product.discount_percentage}%
      </span>
    )}
  </div>

  <!-- Trạng thái kho hàng -->
  <div class="flex items-center gap-2">
    <span class={`inline-flex items-center gap-1 px-3 py-1 rounded-full text-sm font-medium ${availabilityInfo.bgColor} ${availabilityInfo.color}`}>
      <span>{availabilityInfo.icon}</span>
      {availabilityInfo.text}
    </span>
    {availability === 'limited_availability' && product.inventory_quantity && (
      <span class="text-sm text-gray-500">
        (Chỉ còn {product.inventory_quantity} sản phẩm)
      </span>
    )}
  </div>

  <!-- Mô tả ngắn -->
  {product.description && (
    <div class="text-gray-700 leading-relaxed">
      {product.description}
    </div>
  )}

  <!-- Nút mua hàng -->
  <div class="flex gap-4">
    <ProductBuyNowIsland
      client:load
      product={{
        name: product.name || '',
        price: product.price || 0,
        original_price: product.original_price || 0,
        image_url: product.image_url || ''
      }}
    />
    <a href={import.meta.env.PUBLIC_ZALO_URL || 'https://zalo.me/0962916488'} target="_blank" rel="noopener nofollow" class="flex-1 bg-[#1182FC] text-white px-6 py-3 rounded-lg hover:bg-[#0e6fd8] transition-colors flex items-center justify-center gap-2">
      <img src="/images/zalo-2.png" alt="Chat Zalo" class="h-5 w-5 object-contain" />
      Chat Zalo
    </a>
  </div>

  <!-- Chính sách mua hàng -->
  <div class="bg-white rounded-lg shadow-sm p-6">
    <h2 class="text-xl font-semibold mb-4 text-red-700">Chính sách mua hàng tại SOLAR24H</h2>
    <div class="space-y-4">
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0 w-6 h-6 bg-red-100 rounded-full flex items-center justify-center">
          <svg class="w-4 h-4 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
          </svg>
        </div>
        <div>
          <h3 class="font-semibold text-gray-900">Bảo hành chính hãng</h3>
          <p class="text-sm text-gray-600">Tất cả sản phẩm đều được bảo hành chính hãng 12 tháng</p>
        </div>
      </div>
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0 w-6 h-6 bg-red-100 rounded-full flex items-center justify-center">
          <svg class="w-4 h-4 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div>
          <h3 class="font-semibold text-gray-900">Giao hàng nhanh</h3>
          <p class="text-sm text-gray-600">Giao hàng trong 1-3 ngày làm việc</p>
        </div>
      </div>
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0 w-6 h-6 bg-red-100 rounded-full flex items-center justify-center">
          <svg class="w-4 h-4 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div>
          <h3 class="font-semibold text-gray-900">Đổi trả dễ dàng</h3>
          <p class="text-sm text-gray-600">Đổi trả trong vòng 30 ngày nếu sản phẩm còn nguyên vẹn</p>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
function addToCart() {
  // TODO: Implement add to cart functionality
  console.log('Add to cart clicked');
}

function buyNow() {
  // TODO: Implement buy now functionality
  console.log('Buy now clicked');
}

document.getElementById('addToCart')?.addEventListener('click', addToCart);
document.getElementById('buyNow')?.addEventListener('click', buyNow);
</script> 